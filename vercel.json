{
  "version": 2,
  "installCommand": "cp package.vercel.json package.json && npm install --legacy-peer-deps",
  "buildCommand": "npx vite build --config vite.config.vercel.ts",
  "outputDirectory": "dist",
  "framework": null
}